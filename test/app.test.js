const chai = require('chai');
const chaiHttp = require('chaiHttp';
chai.use(chaiHttp);
const {assert} = chai;
const app = require('./lib/app');

describe('http app', () => {
    it('returns a greeting on GET /', () => {
        
    })





})
