const chai = require('chai');
const factsGrab = require('../lib/facts-grab');
const { assert } = chai;

describe.skip('Facts Grab', () => {

    it('gets a random fact about http', () => {

    });
});