// return fact as an object with a fact property make sure  of Content-Type = `application/json`


// Responds with one random of (at least) three interesting facts about http 


/*randomly returned interesting fact, just test the word http as a regex:

    assert.ok(/http/.test(response));

    * Use chai-http to E2E test the API.
    */
//module.exports = facts;

